package Persons;

import java.util.Stack;

public class person<T> {
	
	public String name;
	public String message;
	
	@SuppressWarnings("unchecked")
	public person(T name) {
		person first = new person("");
		@SuppressWarnings("unchecked")
		person second = new person("");
		
	}
	
	public void meet(T first, T second) {	
		if ((first, first) || (second,scond))
			throw RunTimeException;
		else
			
	}
	
	public Boolean knows(T first, T second) {
		if ((first, first) || (second,scond))
			throw RuntimeException;
		else 
			if(first)
		
		
		return true;
		
	}
	
	@SuppressWarnings("unchecked")
	public void post(T message) {
		Stack<T> fmessage = new Stack<T>();
		Stack<T> smessage = new Stack<T>();
		fmessage.push((T) "");
		smessage.push((T) "");
		
		
	}
	
	public void listmessage() {
		while (!fmessage = isempty()) {
			message.pop
		}
			
		
	}
	
}
